# Local helper scripts — used on your computer, never published to the site
deploy.sh
deploy.bat
update_site.py

# transfer archives + helper (root level only) — never published
/*.zip
apply-update.bat

# OS clutter
.DS_Store
Thumbs.db
desktop.ini
_to_delete/
